# Night Cardiology Sentinel - Requirements
# Core dependencies for the Streamlit app

# Streamlit for web interface
streamlit>=1.31.0

# Hugging Face for model downloads
huggingface-hub>=0.20.0

# Data processing
numpy>=1.24.0

# Note: llama-cpp-python requires C++ compiler to build from source
# Install separately with one of these commands:
# For CPU: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# For CUDA (NVIDIA GPU): pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# For Metal (Apple Silicon): pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/metal

# Optional: If using HTTP API instead of llama-cpp-python
# requests>=2.31.0
